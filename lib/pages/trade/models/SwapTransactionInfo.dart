class SwapTransactionInfo {
  final double? minimumReceived;
  final double? priceImpact;
  final double? receiveAmount;
  final double? price;
  final double? lpFee;

  SwapTransactionInfo(this.minimumReceived, this.priceImpact,
      this.receiveAmount, this.price, this.lpFee);
}
