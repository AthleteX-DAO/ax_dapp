import 'package:flutter/material.dart';

class DesktopFarm extends StatelessWidget {
  const DesktopFarm({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Farm page unavailable'));
  }
}
