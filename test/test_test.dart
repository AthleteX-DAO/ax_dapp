import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Testing unit tests', () {
    const actual = 'This should execute successfully';
    const matcher = 'This should execute successfully';
    expect(actual, matcher);
  });
}
