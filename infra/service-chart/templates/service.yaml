{{ $expose := default dict .Values.service.expose }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "dapp.fullname" . }}
  labels:
    {{- include "dapp.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.containerPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dapp.selectorLabels" . | nindent 4 }}
